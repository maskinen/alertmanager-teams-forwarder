apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: development

resources:
- deployment.yaml
- service.yaml

images:
- name: "registry.example.com/library/alertmanager-teams-forwarder"
  newTag: latest
