apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: alertmanager-teams-forwarder-dev
  namespace: argocd
spec:
  destination:
    namespace: development
    server: https://kubernetes.default.svc
  project: default
  syncPolicy:
    automated: {}
  source:
    repoURL: 'ssh://git-user@gerrit.example.com:29418/kubernetes'
    targetRevision: development/alertmanager-teams-forwarder
    path: deployments/alertmanager-teams-forwarder/development